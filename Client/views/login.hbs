<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SAML Client Login</title>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Roboto', sans-serif;
            background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }

      

        /* Logo styling */
        .logo {
            position: absolute;
            top: 20px;
            left: 20px;
            
            
        }

        .logo img {
            max-width: 100px;
            height: auto;
        }

        .login-container {
            background: white;
            padding: 40px;
            border-radius: 12px;
            box-shadow: 0 8px 24px rgba(0, 0, 0, 0.1);
            text-align: center;
            max-width: 400px;
            width: 100%;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .login-container:hover {
            transform: translateY(-5px);
            box-shadow: 0 12px 28px rgba(0, 0, 0, 0.15);
        }

        .login-container h1 {
            font-size: 24px;
            color: #343a40;
            margin-bottom: 20px;
        }

        .login-button {
            background: linear-gradient(90deg, #007bff, #0056b3);
            color: white;
            padding: 12px 24px;
            border: none;
            border-radius: 8px;
            font-size: 16px;
            cursor: pointer;
            transition: background 0.3s ease;
            width: 100%;
        }

        .login-button:hover {
            background: linear-gradient(90deg, #0056b3, #003d7a);
        }

        .login-button:focus {
            outline: none;
        }

        .login-button:active {
            transform: scale(0.98);
        }

        .error-message {
            color: red;
            margin-top: 10px;
            display: none;
            font-size: 14px;
        }

        footer {
            margin-top: 20px;
            font-size: 12px;
            color: #868e96;
        }
    </style>
</head>

<body>

    
<div class="logo">
    <img src="https://cdn.worldvectorlogo.com/logos/saml-1.svg" alt="SAML Logo">
</div>
<div class="login-container">
    <h1>Login with SAML</h1>
    <form id="saml-form" method="post" action="/sp/login-post" autocomplete="off">

        <button class="login-button" id="login-button">Login with SAML</button>
    </form>
    <p class="error-message" id="error-message">An error occurred while logging in. Please try again.</p>
    <footer>Powered by SAML Client</footer>
</div>
   


    <script>
       


    </script>

</body>

</html>